#include "main.h"
/**
 * add - Fucntion that adds two integers and returns the results
 * @a: Number one to be computed
 * @b: Number 2 to be computed
 * Return: Integer value
 */
int add(int a, int b)
{
return (a + b);
}
