#include <stdio.h>
/**
 * main - Program that prints the alphabet in lowercase
 * Return: 0 (Success)
 */
int main(void)
{
	char c;
while(c = 'a' ; c <= 'z' ; c++)
putchar(c);
}
putchar('\n');
return (0);
}
